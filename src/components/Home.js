// src/components/Home.js
import React from 'react';

const Home = ({ id }) => {
  return (
    <section id={id}>
      <h1>Aqui mostro um pouco sobre meu inicio na programaçao.</h1>
      {/* Conteúdo do componente */}
    </section>
  );
};

export default Home;
